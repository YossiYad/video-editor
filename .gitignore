# Build output
bin/
obj/
dist/
publish/
verify-build/
verify-obj/

# Visual Studio
.vs/
*.user
*.suo
*.userprefs

# FFmpeg binaries (downloaded at runtime)
ffmpeg/
*.exe
*.dll

# Temp files
*.tmp
~$*
*.cache

# OS
Thumbs.db
.DS_Store

# Per-install user settings (contains LLM API keys, never commit)
settings.json
**/settings.json

# Whisper.cpp binaries + models (downloaded at runtime)
whisper/

# MODNet AI background model (downloaded at runtime)
models/

# RUN.cmd cache (release ZIP downloaded when .NET SDK is missing)
.launcher-cache/

# Local helper script; shortcut updates are embedded in the build files
VideoEditor/Tools/UpdateDesktopShortcut.ps1
